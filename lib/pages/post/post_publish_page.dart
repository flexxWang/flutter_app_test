import 'package:flutter/material.dart';

class PostPublishPage extends StatelessWidget {
  const PostPublishPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('发布动态')));
}
